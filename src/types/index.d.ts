// Shared ambient/shared types go here as the app grows. Prisma already
// generates types for all DB models (import from "@prisma/client"), so this
// file intentionally starts empty rather than duplicating them.
export {};
